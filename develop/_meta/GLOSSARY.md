---
created: 2026-02-01
updated: 2026-02-20
tags: [meta, glossary]
status: done
---

# 용어 정의집

이 문서는 `develop` 폴더 내 문서에서 사용되는 주요 용어를 정의합니다. 새로운 용어가 등장할 때마다 이 문서를 업데이트하여 일관된 용어 사용을 유지합니다.

---

## 사용 규칙

1. **용어 등록 기준**: 문서에서 처음 등장하는 기술 용어, 약어, 개념
2. **형식**: `**한글명** (English) - 정의`
3. **정렬**: 가나다순 (ㄱ-ㅎ), 이후 알파벳순 (A-Z)
4. **참조 방법**: 문서 내에서 `[[GLOSSARY#용어명]]`으로 링크

---

## ㄱ

**검색 증강 생성** (Retrieval-Augmented Generation, RAG) - LLM이 외부 지식 베이스에서 관련 정보를 검색하여 답변 품질을 높이는 기술 프레임워크

**구체화** (Concretization) - 추상화의 반대 개념. 추상적인 개념이나 인터페이스를 실제 구현으로 구체화하는 과정

**그래프 RAG** (GraphRAG) - 지식 그래프를 활용하여 개체 간 관계를 구조적으로 탐색하는 검색 증강 생성 기법

---

## ㄴ

**낙관적 동시성 제어** (Optimistic Concurrency Control) - 충돌이 드물다는 가정하에 락 없이 작업을 수행하고, 커밋 시점에 충돌을 감지하여 처리하는 동시성 제어 방식. 버전 번호나 ETag를 활용

---

## ㄷ

**단일 실패 지점** (Single Point of Failure, SPOF) - 해당 구성 요소가 실패하면 전체 시스템이 중단되는 지점. 이중화로 방지해야 함

---

## ㄹ

**리랭킹** (Re-ranking) - 초기 검색 결과를 재정렬하여 가장 관련성 높은 문서를 선별하는 기법

---

## ㅁ

**멱등성** (Idempotency) - 동일한 연산을 여러 번 수행해도 결과가 한 번 수행한 것과 동일한 성질. 분산 시스템에서 안전한 재시도와 데이터 일관성을 보장하는 핵심 원칙

**멱등성 키** (Idempotency Key) - 클라이언트가 생성하는 고유 식별자로, 서버가 동일 요청의 재시도를 감지하고 중복 처리를 방지하는 데 사용

---

## ㅂ

**배치 처리** (Batch Processing) - 대량의 데이터를 일괄적으로 처리하는 방식. 실시간 처리와 대비되며, 비용 효율적인 대량 작업에 적합

**벌크 처리** (Bulk Processing) - 여러 건의 데이터를 개별 처리 대신 한 번에 묶어서 처리하는 방식. N개 요청을 소수의 쿼리로 최적화

**벡터 데이터베이스** (Vector Database) - 고차원 벡터 데이터를 저장하고 유사도 기반 검색을 수행하는 특수 목적 데이터베이스

---

## ㅅ

**서킷 브레이커** (Circuit Breaker) - 장애가 발생한 서비스로의 호출을 일시 차단하여 장애 전파를 방지하는 패턴

**스레드** (Thread) - 프로세스 내에서 실행되는 흐름의 단위. 프로세스의 자원을 공유하며 스택만 독립적으로 가짐

**Strategy Pattern** - 동일한 문제를 해결하는 여러 알고리즘을 정의하고 런타임에 교체할 수 있도록 하는 패턴. 도메인 계층에 위치

**싱글톤** (Singleton) - 클래스의 인스턴스가 하나만 생성되도록 보장하는 패턴. 리소스 재사용이 필요할 때 사용

**SOLID** - 객체지향 설계의 5가지 원칙(SRP, OCP, LSP, ISP, DIP)의 앞글자를 딴 약어

---

## ㅇ

**일반화** (Generalization) - 여러 구체적인 대상들로부터 공통점을 추출하여 상위 개념을 형성하는 과정. 상속이나 부모 클래스로 구현

**임베딩** (Embedding) - 텍스트, 이미지 등의 데이터를 고차원 벡터 공간에 수치로 표현하는 방법

---

## ㅈ

**지식 그래프** (Knowledge Graph) - 개체(노드)와 관계(엣지)로 구성된 구조화된 데이터 표현으로, 정보 간의 연결 관계를 명시적으로 표현하는 그래프

---

## ㅊ

**추상화** (Abstraction) - 복잡한 내부 구현을 숨기고 사용자에게 필요한 핵심만 노출하여 단순화하는 과정. 인터페이스나 추상 메서드로 구현

**청킹** (Chunking) - 긴 문서를 검색 및 처리에 적합한 작은 단위로 분할하는 전략

---

## ㅊ

**컨텍스트 스위칭** (Context Switching) - CPU가 한 프로세스/스레드에서 다른 것으로 전환할 때 상태를 저장하고 복원하는 과정

**컨테이너** (Container) - 애플리케이션과 그 실행 환경을 격리된 단위로 패키징하는 가상화 기술

**컨테이너 오케스트레이션** (Container Orchestration) - 다수의 컨테이너 배포, 관리, 확장, 네트워킹을 자동화하는 기술. ECS, Kubernetes 등이 대표적

---

## ㅋ

---

## ㅌ

**특수화** (Specialization) - 일반화의 반대 개념. 상위 개념을 구체적인 하위 개념으로 세분화하는 과정

---

## ㅍ

**Provider Pattern** - 외부 리소스나 API와의 연동을 추상화하는 패턴. 인프라 계층에 위치하며 싱글톤으로 관리

**Factory Pattern** - 객체 생성 로직을 캡슐화하여 클라이언트가 구체 클래스를 알 필요 없이 적절한 객체를 얻게 하는 패턴

**파인튜닝** (Fine-tuning) - 사전 학습된 모델을 특정 도메인이나 작업에 맞게 추가 학습시키는 방법

**프로세스** (Process) - 운영체제로부터 자원을 할당받는 작업의 단위. 독립적인 메모리 공간을 가짐

**프롬프트** (Prompt) - LLM에 입력으로 제공되는 텍스트 지시문

---

## ㅎ

**할루시네이션** (Hallucination) - LLM이 사실이 아닌 정보를 마치 사실인 것처럼 생성하는 현상

---

## A-Z

**API** (Application Programming Interface) - 소프트웨어 간 상호작용을 위한 인터페이스 정의

**API Gateway** - MSA에서 클라이언트와 내부 서비스 사이의 단일 진입점. 라우팅, 인증, 트래픽 제어 등 공통 기능을 처리

**ASGI** (Asynchronous Server Gateway Interface) - 파이썬 비동기 웹 애플리케이션과 서버 간의 표준 인터페이스. WSGI의 비동기 확장으로, 웹소켓과 스트리밍을 지원

**BFF** (Backend for Frontend) - 클라이언트 유형(웹, 모바일 등)별로 최적화된 API를 제공하기 위해 게이트웨이를 분리하는 패턴

**Docker** - 컨테이너 기반 애플리케이션 배포 및 관리를 위한 오픈소스 플랫폼

**ECR** (Elastic Container Registry) - AWS의 관리형 컨테이너 이미지 저장소

**ECS** (Elastic Container Service) - AWS의 완전 관리형 컨테이너 오케스트레이션 서비스

**EKS** (Elastic Kubernetes Service) - AWS의 관리형 Kubernetes 서비스

**Fargate** - AWS의 서버리스 컨테이너 컴퓨팅 엔진. ECS/EKS와 함께 사용하여 인프라 관리 없이 컨테이너 실행

**IPC** (Inter-Process Communication) - 프로세스 간 데이터를 주고받는 통신 기법. 파이프, 소켓, 공유 메모리 등이 있음

**JSONL** (JSON Lines) - 각 줄이 하나의 JSON 객체인 텍스트 형식. 배치 API에서 대량 요청/응답 처리에 사용

**LLM** (Large Language Model) - 대규모 텍스트 데이터로 학습된 자연어 처리 인공지능 모델

**MSA** (Microservices Architecture) - 애플리케이션을 독립적으로 배포 가능한 작은 서비스들의 집합으로 구성하는 아키텍처 스타일

**UPSERT** - INSERT와 UPDATE의 합성어. 레코드가 존재하면 갱신하고, 존재하지 않으면 삽입하는 원자적 데이터베이스 연산. 멱등적 데이터 처리에 활용

---

## 관련 문서

- [[RULES]] - 문서 작성 규칙
- [[INDEX]] - 전체 문서 목록
